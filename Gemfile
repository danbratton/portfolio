source "https://rubygems.org"

# GitHub Pages builds against a pinned gem set. Using the meta-gem keeps the
# local preview aligned with whatever GitHub Pages currently serves.
gem "github-pages", group: :jekyll_plugins

# Platform-specific gems Jekyll needs on newer Rubies.
gem "webrick", "~> 1.8"
